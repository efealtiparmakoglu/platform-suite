apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: platform-suite-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: platform-suite.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: platform-suite-service
            port:
              number: 80
